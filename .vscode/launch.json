{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Android: Run on Emulator",
      "type": "pwa-node",
      "request": "launch",
      "program": "${workspaceFolder}/.vscode/noop.js",
      "preLaunchTask": "Android: Build + Install + Launch",
      "console": "integratedTerminal"
    }
  ]
}
